---
import PostTag from "./PostTag.astro";
const {tags} = Astro.props;
---
<div class="pb-[10px]">
    {
        !!tags ?
            tags.map((tag :String) => <PostTag tag={tag} />)
            : ""
    }
</div>
