---
import BaseLayout from '../../layouts/BaseLayout.astro';
import {getCollection} from "astro:content";
const micros = await getCollection("micros");
micros.sort((a, b) => {
    return a.data.date > b.data.date ? -1 : 1;
})
import MicroPost from "@components/Micros/MicroPost.astro";
import onTheJohn from "@contentsubmodule/images/OnTheJohn.png";
const pageTitle = "Micro Posts";
---
<BaseLayout pageTitle={pageTitle}>
    <p>
        You may remember the microblog, sites that had things like birds on their logos. You'd post your lunch, or little
        jokes and other people would comment on them. This is my one-way microblog: I post my brief thoughts, you read.
        Anti-social media, if you will.
    </p>
    <div class="micro-container">
        {
            micros.map(micro => (<MicroPost micro={micro} avatar={onTheJohn} singlePage={false} />))
        }
    </div>
</BaseLayout>
<style>

</style>